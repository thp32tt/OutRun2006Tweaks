name: Source-only 30-pass FFB review

on:
  push:
    branches: [ wheel-ffb ]
    paths:
      - '.github/workflows/source-ffb-review30.yml'

permissions:
  contents: write

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout wheel-ffb
        uses: actions/checkout@v7.0.1
        with:
          ref: wheel-ffb
          fetch-depth: 0

      - name: Configure review commits
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Apply only justified review fixes
        run: python3 .github/source_ffb_review30.py

      - name: Push reviewed source commits
        run: git push origin wheel-ffb

# Rerun marker: corrected exact source replacements.
