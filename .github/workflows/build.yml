name: Build

on:
  workflow_dispatch:

jobs:
  source-external-review-hold:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Compiled build temporarily paused for second external FFB source review"
