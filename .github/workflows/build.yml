name: Build

on:
  workflow_dispatch:

jobs:
  source-review-hold:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Compiled build temporarily paused for detailed 30-pass profile review"
