name: Build

on:
  workflow_dispatch:

jobs:
  source-research-hold:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Compiled build temporarily paused for research-informed FFB source update"
