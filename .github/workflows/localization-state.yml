name: Localization State (Legacy Prototype Frozen)

on:
  workflow_dispatch:

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy branch notice
        run: |
          echo "korean-localization-prototype is frozen."
          echo "Use korean-localization-clean for all active localization development."
