# A Deep Review — A-20260921T072900KST-manual01

Base integration SHA: `e5e35f483cf7f8d36f99a87807ba6cb9d4b7eccd`

Status: **COMPLETE**  
Evidence: **STATICALLY VERIFIED**. No production runtime source changed and no HMD correctness claim is made.

## READY: DX9EX-DISPATCH-XYZRHW-001

The active final R34/R33 draw topology bypasses R30's bounded XYZRHW owner. R30 has specialized handlers for all four D3D9 draw families, gated by a null vertex shader + `D3DFVF_XYZRHW` and then a projection-consistent Z/RHW signature that separates CPU-projected smoke/skid/decal effects from white HUD/rank/score-style glyphs. R31/R32/R33 do not call those handlers; R33's lower route invokes the R29 trampoline directly.

**B direction:** call the matching bounded `R30TryXyzrhw*` at the final owner before generic HUD/world dispatch. A handled result returns exactly once; only `E_NOTIMPL` continues. Do not recursively route the whole owner through the old R30 wrapper and do not broaden WVP ownership.

**Verifier:** source-flow/handled-result test for all four draw families. LEVEL0 build/static first; visual result remains LEVEL1 HMD.

## READY: DX9EX-SKYGLOW-001

`R30ApplyStereoSkyGlow` has reversed composite-source selection. Actual flow is bright `reduced -> temp`, horizontal blur `temp -> reduced`, optional vertical blur `reduced -> temp`. Therefore one-step must composite `reduced`, and two-step must composite `temp`. Current code initializes the source to `temp` and changes it to `reduced` after vertical blur.

At the requested `SkyGlowFactor=1`, effective two-step is intentionally off, so the current path composites pre-blur bright extraction and skips the horizontal-blur result.

**B direction:** initialize composite source to `reduced`; after a successful vertical pass set it to `temp`. Add deterministic pass-flow verification.

## READY: DX9EX-PERF-DISPATCH-001

`R33Dispatch` calls `R30ClassifyScreenSpacePass`, then `R33TryHud` immediately calls the same classifier again. Perspective flat effects therefore duplicate projection lookup plus Z/alpha render-state reads.

**B direction:** pass the already-computed `R30ScreenSpaceKind` into `R33TryHud`. This is a small LEVEL0 hot-path cleanup, not a visual claim.

## READY: DX9EX-RESET-STATEBLOCK-001

The D3D9Ex Reset lifecycle contains a resource-lifetime defect worth fixing before treating Reset/recenter recovery as robust.

`R15CaptureClassicExtraBaseline` creates an `IDirect3DStateBlock9` using `CreateStateBlock(D3DSBT_ALL)` on the fresh promoted device and stores that COM object in `R15ClassicAllState`. Later `ResetCompatDeviceR15` performs `ResetEx` through the R13 path and then calls `R15RestoreClassicAllState`, whose implementation invokes `Apply()` on that same state-block object captured before ResetEx.

A state-block object is a device resource, not a durable serialized state snapshot. Keeping the COM object alive across ResetEx and using it as the post-reset restoration oracle is therefore the wrong lifetime contract. The surrounding implementation already has explicit baseline values (`ClassicBaseline` plus `R15ClassicExtra`) and a fail-closed `R15ResetStateHealthy` / external safety block, so this can be corrected without weakening safety.

**B direction:** remove reliance on a pre-Reset state-block object. Prefer an explicit fresh-device baseline representation for the state families currently delegated to D3DSBT_ALL, or recreate/capture an equivalent valid state block after ResetEx before any Apply. Keep the existing health bit fail-closed if restoration cannot be proven.

**Verifier:** deterministic source test must reject `Apply()` of an object whose capture generation predates ResetEx. Build validation LEVEL0; actual device-loss/reset behavior remains LEVEL3/HMD/backend evidence.

## D3D9Ex lifecycle review — no additional promotion blocker found

The CreateDeviceEx transaction itself is intentionally conservative: original `D3DPRESENT_PARAMETERS` are copied before the Ex attempt, classic fallback restores that copy, PUREDEVICE skips promotion, third-party d3d9 providers skip promotion, R15 must finish compatibility and synchronous stereo-hook handoff before the promoted device escapes, and failed handoff rejects the Ex device. The R14 managed-2D shadow registry is lifetime-bound rather than fixed-capacity and defers retirement through matching Unlock when necessary.

The new finding is specifically the persisted `D3DSBT_ALL` object across ResetEx, not a reason to undo the broader promotion transaction.

## Five-lens result

- **Architecture/control flow:** final R33/R34 topology drops the R30 XYZRHW owner.
- **Resource lifetime/reset:** pre-Reset `IDirect3DStateBlock9` is reused after ResetEx; replace that contract.
- **Stereo/visual correctness:** bounded XYZRHW classifier directly targets unresolved white-overlay and smoke/skid classes; SkyGlow selects the wrong intermediate.
- **Hot path:** R33 duplicates screen-space classification; bounded fixes avoid broad per-draw work.
- **Adversarial falsification:** R31/R32/R33 contain no alternate XYZRHW route; R31 StateBlock resync is live; R14 shadow registry is not the old overwrite ring; cadence and DirectGPU per-slot fencing hypotheses were rechecked and not promoted as defects.

## Next action

B may now consume this handoff because the run is COMPLETE. Preferred isolated candidates: (1) `DX9EX-DISPATCH-XYZRHW-001`, (2) `DX9EX-RESET-STATEBLOCK-001`, (3) `DX9EX-SKYGLOW-001`, then the small `DX9EX-PERF-DISPATCH-001`. C validates immutable candidate SHAs; visual/runtime conclusions stay unclaimed until Quest 3/VDXR evidence exists.
