# C implementation handoff — 2026-09-23 05:35 KST

- status: BLOCKED_SAFE_PATCH_CAPABILITY
- selectedFinding: DX9EX-WRAPPER-QI-ESCAPE-001
- base_sha: 539fecf218d39cea103da6ccc23f59eb7498185f
- candidate_sha: null
- integrationBranch: vr-d3d9ex-focus
- productionWrite: false
- correctiveRevisions: 0
- CI: not triggered (no candidate)

## Root cause / RED
Current `src/vr/d3d9/ex_device_upgrade.cpp::Direct3D9ExCompat::QueryInterface` exposes the underlying system `IDirect3D9Ex` for `IID_IDirect3D9Ex`, permitting callers to bypass wrapper `CreateDevice` and therefore the managed-resource/stereo compatibility transaction. The bounded GREEN remains fail-closed `E_NOINTERFACE` for `IID_IDirect3D9Ex` while preserving wrapper IUnknown/IDirect3D9 identity and legacy CreateDevice promotion.

## SAFE PATCH recovery
C0 recovered exact integration HEAD and protocol/regression registry. The GitHub connector can fetch the immutable source blob, including base64, but the response is truncated before the complete 42KB+ production file is available to the model. The available write operation is whole-file replacement. Reconstructing the production file from truncated/chunked excerpts would violate SAFE PATCH, so no candidate branch or production edit was published.

## BASELINE_DELTA
Planned path `src/vr/d3d9/ex_device_upgrade.cpp` intersects `VR-STARTUP-WHITE-001` via `src/vr/**`. Protected USER_RUNTIME baseline remains R51 source `17ad376bfdf7939f0851c0c629e4fa094a84f28a`; build evidence must not advance it. Preserve world stereo, frame stability, deferred stereo-resource initialization, SkyGlowFactor=1, TargetRefreshRateHz=0, and no broad world-to-HUD ownership.

## Required verifier
1. RED on exact base: hooked Direct3DCreate9 object QI(IDirect3D9Ex) succeeds/raw escape is observable.
2. GREEN: QI(IDirect3D9Ex) returns E_NOINTERFACE; IUnknown/IDirect3D9 identity remains stable.
3. Legacy CreateDevice still promotes through CreateDeviceEx and compatibility transaction.
4. Build active DX9Ex DLL + host, host smoke tests, regression-knowledge verifier.
5. Because src/vr/** intersects startup protection, carry VR-STARTUP-WHITE-001 as triggered; runtime-visible final DONE still requires Quest3/VDXR evidence if behavior changes.

## nextAction
Acquire a lossless complete immutable file/patch-capable write path, then create a fresh `vr-d3d9ex-candidate/DX9EX-WRAPPER-QI-ESCAPE-001-<run>` from the exact then-current integration HEAD; rerun RED, apply only the QueryInterface hunk plus deterministic verifier, prove GREEN, compare base..candidate intended paths, trigger candidate CI, and hand exact SHA/run to D.
