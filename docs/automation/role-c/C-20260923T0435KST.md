# Role C handoff — 2026-09-23 04:35 KST

status: BLOCKED
selected_item: DX9EX-WRAPPER-QI-ESCAPE-001
base_sha: 539fecf218d39cea103da6ccc23f59eb7498185f
candidate_sha: null
candidate_branch: null

## Root cause
`src/vr/d3d9/ex_device_upgrade.cpp::Direct3D9ExCompat::QueryInterface` returns the raw `IDirect3D9Ex` interface for `IID_IDirect3D9Ex`. A caller can therefore bypass the wrapper's legacy `CreateDevice -> CreateDeviceEx -> InstallManagedResourceCompat` transaction. Bounded fix intent is fail-closed `E_NOINTERFACE` for `IID_IDirect3D9Ex` while preserving wrapper `IUnknown/IDirect3D9` identity.

## TDD / RED -> GREEN
RED evidence on exact base: QueryInterface contains `if (riid == __uuidof(IDirect3D9Ex) && ex_) return ex_->QueryInterface(riid, object);`.
GREEN oracle: wrapper must return E_NOINTERFACE for IDirect3D9Ex, continue returning itself for IUnknown/IDirect3D9, and keep CreateDevice promotion path unchanged. Add deterministic source/architecture assertion before publication.

## BASELINE_DELTA
Protected runtime baseline remains R51 `17ad376bfdf7939f0851c0c629e4fa094a84f28a`. Planned path `src/vr/d3d9/ex_device_upgrade.cpp` intersects `VR-STARTUP-WHITE-001` via `src/vr/**` and device-creation revalidation trigger. It does not intersect the explicit R51 world-stereo path list, but startup/device promotion must be revalidated. BUILD evidence cannot advance runtime baseline. Final runtime-visible DONE remains USER_RUNTIME_REQUIRED if startup behavior changes.

Protected invariants: preserve deferred stereo resource initialization after successful game Present; preserve R51 road/background/vehicle stereo and improved frame stability; do not broaden world-to-HUD ownership; preserve SkyGlowFactor=1 and TargetRefreshRateHz=0.

## Safe-patch blocker
Complete immutable source identity is blob `05ad9eb798c89bdea42e9e41cbd438369d9e7eec` (42549 bytes), and the active QueryInterface hunk was re-read from exact base. Available write action replaces the complete UTF-8 file; the connector does not expose a bounded hunk/patch write and the complete 42.5 KB source is not available to the model as one lossless response. Reconstructing the production file from excerpts/chunks would violate SAFE PATCH. No production candidate was created and no source was modified.

failure_classification: permissions/capability block (safe bounded production patch unavailable), not product failure.
corrective_revisions: 0
CI: not triggered because no candidate exists.
TEST_LEVEL: LEVEL0 deterministic/build + preserve LEVEL1 runtime baseline gate.
required_post_review: A for device/wrapper lifetime; B only if rendering behavior changes unexpectedly; D exact-SHA review after candidate exists.

exact_nextAction: On a run with a safe exact-blob patch/materialization path, branch from the then-current `vr-d3d9ex-focus` SHA; re-read regression registry/baseline; run RED assertion; apply only the IDirect3D9Ex QueryInterface fail-closed hunk plus deterministic verifier; run GREEN; compare base..candidate for intended paths only; trigger candidate DX9Ex Active Validation immediately; hand exact SHA/run to D. Do not use any earlier partial candidate.