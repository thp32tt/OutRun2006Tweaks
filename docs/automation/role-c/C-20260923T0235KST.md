# Role C handoff — 2026-09-23 02:35 KST

status: BLOCKED
base_sha: 539fecf218d39cea103da6ccc23f59eb7498185f
candidate_branch: vr-d3d9ex-candidate/DX9EX-WRAPPER-QI-ESCAPE-001-C-20260923T0235KST
candidate_sha: null (branch restored to exact base; no partial production edit retained)
finding: DX9EX-WRAPPER-QI-ESCAPE-001
regression_keys: VR-STARTUP-WHITE-001
TEST_LEVEL: LEVEL0 + NEED_HMD_TEST only if later runtime-visible startup/device behavior changes

## C0 recovery / runtime baseline
- userRuntimeVerifiedBaseline source: 17ad376bfdf7939f0851c0c629e4fa094a84f28a (R51 partial accepted baseline).
- Protected invariants: startup progression; correct road/background/vehicle world stereo; improved frame stability; no broad world-to-HUD ownership.
- Current integration 539fecf218d39cea103da6ccc23f59eb7498185f is newer than the runtime baseline and must not replace it by build evidence.
- Open runtime defects remain white HUD diplopia and vehicle-rank world anchoring; this finding is independent architecture hardening and must not be used to claim those fixed.

## Root cause / active path
`Direct3D9ExCompat::QueryInterface` returns the raw underlying `IDirect3D9Ex` for IID_IDirect3D9Ex. A caller can therefore obtain that provider from the hooked legacy Direct3D9 wrapper and call `CreateDeviceEx` without entering `Direct3D9ExCompat::CreateDevice`, bypassing `InstallManagedResourceCompat` and the compatibility/stereo transaction. The wrapper also exposes a split controlling-IUnknown identity through that raw interface.

## RED oracle
On exact base, source inspection proves `QueryInterface(__uuidof(IDirect3D9Ex))` delegates to `ex_->QueryInterface`. Deterministic target: QI on the hooked legacy wrapper must not expose a raw Ex interface unless a real Ex wrapper routes `CreateDeviceEx` through the same compatibility transaction and preserves COM identity.

## Intended minimal GREEN
Fail closed for IID_IDirect3D9Ex from the legacy-only compatibility wrapper (`E_NOINTERFACE`) rather than expose raw `ex_`. Preserve IID_IUnknown/IID_IDirect3D9 identity and the existing legacy `CreateDevice -> CreateDeviceEx -> InstallManagedResourceCompat` path. Do not alter startup/profile/rendering configuration.

## BASELINE_DELTA
Changed-path target `src/vr/d3d9/ex_device_upgrade.cpp` intersects `VR-STARTUP-WHITE-001` via `src/vr/**` and device-creation trigger. Required revalidation after a real candidate exists: architecture guard for deferred stereo initialization, active DX9Ex game build, host smoke/regression registry contract; runtime startup invariant remains protected and cannot be advanced by BUILD_VERIFIED. R51 world-stereo/frame-stability protected paths are not intentionally changed.

## Blocker / self-recovery
The complete immutable blob was successfully materialized (`05ad9eb798c89bdea42e9e41cbd438369d9e7eec`), so the prior truncated-read blocker is resolved. However the connected GitHub write primitive available in this run only replaces an entire file; no bounded hunk/patch action is exposed. C created the isolated candidate branch from the exact base, then deliberately restored it to the exact base without retaining a partial/reconstructed production edit. No candidate CI was triggered because there is no production delta.

nextAction: On the next C run, use a safe exact-blob transformation/write path capable of preserving the complete file, apply only the IID_IDirect3D9Ex fail-closed hunk plus a deterministic verifier, prove RED on base -> GREEN on candidate, compare base..candidate for intended paths only, trigger candidate CI, and hand the exact SHA to D. Do not integrate the current branch because it equals base.
