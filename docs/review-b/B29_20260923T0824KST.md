# B29 — 2026-09-23 08:24 KST

Target integration: `539fecf218d39cea103da6ccc23f59eb7498185f`.
Protected USER_RUNTIME baseline: `17ad376bfdf7939f0851c0c629e4fa094a84f28a`.
Role: B REVIEW-ONLY. Production/candidate source writes: 0.
Accounting: fresh=75, carry=75, total=150. Fresh objectives F01..F75 are new B29 objectives concentrated on under-covered R30 buffer-shadow, XYZRHW, sky-glow/state/copy/performance boundaries. Carry C76..C150 is identity-validated only.
Regression keys reused: `VR-STARTUP-WHITE-001`, `VR-R51-WORLD-STEREO-PRESERVE-001`, `VR-R51-FRAME-STABILITY-PRESERVE-001`, `VR-R51-WHITE-HUD-DIPLOPIA-001`, `VR-R51-VEHICLE-RANK-ANCHOR-001`.

## Fresh evidence units
F01 CROSS R30BeginObservedLock: game Lock success is observed before shadow ownership; CPU bytes are not fabricated when Lock fails.
F02 ADV R30BeginObservedLock: offset>size, zero/overflow effective size are rejected, falsifying unchecked shadow-range ingestion.
F03 PATH R30BeginObservedLock: writeLock is false for D3DLOCK_READONLY, so read-only locks cannot become new authoritative bytes.
F04 REG R30BeginObservedLock: D3DLOCK_DISCARD clears prior valid ranges before accepting replacement bytes, protecting stale XYZRHW geometry from reuse.
F05 PERF R30BeginObservedLock: per-lock entry mutex is a CPU synchronization point; no pacing regression promoted without measured contention.
F06 CROSS R30FinishObservedLock: bytes are snapshotted while Lock pointer is valid and before real Unlock invalidates it.
F07 ADV R30FinishObservedLock: failed/invalid write capture clears validity rather than preserving ambiguous data.
F08 PATH R30FinishObservedLock: allocation/copy exceptions clear bytes+valid ranges, fail-open toward lower renderer rather than partial stereo conversion.
F09 REG R30FinishObservedLock: exact written interval is merged into validity, avoiding whole-buffer trust after partial writes.
F10 PERF R30FinishObservedLock: memcpy cost scales with observed write size and can occur on game write path; measure under R51 frame-stability invariant before broadening interception.
CP10 fresh=10 carry=0 total=10.
F11 CROSS R30MergeValidRange: adjacent/overlapping valid intervals coalesce, allowing later draw reads spanning multiple contiguous writes.
F12 ADV R30MergeValidRange: disjoint gaps remain gaps; a requested draw crossing an unwritten gap cannot be declared valid.
F13 PATH R30RangeValid: requires one range to fully contain requested interval, preserving conservative byte authority.
F14 REG shadow validity: conservative range logic supports fail-open rendering rather than replaying stale/unknown HUD/world vertices.
F15 PERF valid-range vectors can grow under fragmented writes; bounded buffer size limits memory but not interval count, so fragmentation is a diagnostic/perf watch rather than a promoted defect.
F16 CROSS R30CopyVertexShadow: registry lookup then entry mutex creates coherent snapshot against concurrent observed writes.
F17 ADV R30CopyVertexShadow: absent entry, bounds mismatch, byte-size mismatch or invalid range all return false; no fallback GPU READONLY Lock occurs.
F18 PATH R30CopyVertexShadow: first miss logs once via R30FirstShadowMissLogged, bounding repeated diagnostic I/O.
F19 REG vertex-shadow miss: miss delegates to R29 path; static evidence cannot claim stereo correctness of that runtime fallback, retain NEED_HMD_TEST.
F20 PERF vertex copy resizes scratch output and memcpy per converted draw; this is a CPU copy hot-path candidate requiring counters/frame-time comparison.
CP20 fresh=20 carry=0 total=20.
F21 CROSS R30CopyIndexShadow mirrors vertex authority for indexed XYZRHW draws so index topology and vertex bytes share conservative provenance.
F22 ADV R30CopyIndexShadow rejects unknown/partial index bytes instead of inferring indices from vertex count.
F23 PATH R30EnsureIndexShadow accepts only INDEX16/INDEX32 and <=16MiB buffers, bounding unsupported format behavior.
F24 REG indexed path: format gating reduces risk of malformed index expansion corrupting stereo draw ownership.
F25 PERF indexed conversion adds index shadow copy plus used-mask work; compare draw/copy counters against protected frame stability before any expansion.
F26 CROSS R30VertexBufferUnlockDest snapshots before forwarding real Unlock, matching D3D pointer lifetime contract documented in source.
F27 ADV R30VertexBufferUnlockDest clears valid ranges if underlying Unlock later fails, avoiding trust in a write the device rejected.
F28 PATH R30VertexBufferReleaseDest erases registry only when COM refcount reaches zero, avoiding premature shadow lifetime termination.
F29 REG release/registry: shared_ptr acquired by draw protects in-flight shadow while registry entry can be erased.
F30 PERF release cleanup takes global registry mutex only at final release; no evidence of per-draw global erase contention.
CP30 fresh=30 carry=0 total=30.
F31 CROSS R30ScratchLease uses thread_local scratch and busy flag so nested/reentrant conversion cannot overwrite active scratch state.
F32 ADV R30ScratchLease returns no buffers when already busy; nested path must fail open rather than alias vectors.
F33 PATH R30ScratchBuffers separates source/indexSource/left/right/used/physical/index arrays, avoiding left/right output alias by construction.
F34 REG scratch reentrancy: conservative lease is compatible with preserving world stereo instead of attempting unsafe nested duplication.
F35 PERF thread_local vector reuse avoids routine allocation after capacity stabilizes; peak capacity retention is a memory watch, not a correctness finding.
F36 CROSS R30PrepareXyzrhwState requires R29StableStereoBase plus vertex-shader identity zero and actual GetVertexShader null, tying conversion to fixed-function XYZRHW only.
F37 ADV R30PrepareXyzrhwState rejects non-XYZRHW FVF and invalid viewport/depth range, falsifying generic pretransformed-draw interception.
F38 PATH R30PrepareXyzrhwState binds current latched stereo pose and rejects nonzero FrameStereoPoseSequence mismatch.
F39 REG pose identity: stale pose cannot silently drive XYZRHW eye transform; runtime-visible placement remains NEED_HMD_TEST.
F40 PERF EnsureStereoResources is on eligibility path; deferred-startup regression key must remain protected if candidate changes resource timing.
CP40 fresh=40 carry=0 total=40.
F41 CROSS R30XyzrhwHasProjectedDepthSignature correlates screen Z, RHW, viewport depth span and renderer projection rather than classifying by color/alpha.
F42 ADV depth signature: RHW near 1 or incoherent projected depth does not qualify as world, directly guarding white HUD/rank glyph false promotion.
F43 PATH depth signature samples at most 512 used vertices and requires >=3 sampled/valid with >=75% projected coherence.
F44 REG classification: this evidence supports preserving R51 world/HUD separation; it does not solve exact SCREEN_HUD ownership by itself.
F45 PERF 512-vertex cap bounds per-draw classification arithmetic; indexed used-mask traversal cost remains a runtime counter target.
F46 CROSS R30ConfigureXyzrhwWorldEffect permits Z-disabled CPU-projected smoke/skid only when strengthened Z/RHW relation supplies world evidence.
F47 ADV ConfigureXyzrhwWorldEffect rejects depth-enabled draws lacking RHW depth evidence, preventing depth state alone from promoting HUD to world.
F48 PATH ConfigureXyzrhwWorldEffect requires synchronized right depth/stencil when corresponding tests are active, otherwise fails closed to world conversion.
F49 REG smoke/skid: exact effect placement is runtime-visible and cannot be marked fixed from this static gate; NEED_HMD_TEST.
F50 PERF TryBootstrapRightDepthFromRecentClear is conditional on tracked unsynchronized depth/stencil; monitor bootstrap frequency rather than assume zero cost.
CP50 fresh=50 carry=0 total=50.
F51 CROSS full-world reprojection reconstructs view-space point from base projection then applies eye-relative translation and asymmetric eye projection.
F52 ADV eye transform intentionally uses identity orientation plus eye offset relative to common center; source camera already owns common head rotation, preventing double head rotation.
F53 PATH worldScale/worldOffset/parallax fallback values are finite/range checked before use.
F54 REG smoke/skid placement: preserving head-relative IPD-only transform is a baseline-sensitive boundary for world effects and vehicle-adjacent particles.
F55 PERF matrix inversion/projection setup is per classified draw state preparation; no frame-time claim without measured draw frequency.
F56 CROSS R30EnsureSkyGlowResources keys cache by eye/backbuffer dimensions, glow dimensions and factor, so resize/factor changes force resource rebuild.
F57 ADV SkyGlow factor is clamped 1..16 internally; checked-in factor 4 is still policy-wrong for CORRECTNESS despite being numerically safe.
F58 PATH glow dimensions floor at 160x120, so very small eyes/factor combinations cannot shrink below that workload.
F59 REG SkyGlowFactor contract remains 1 for CORRECTNESS; current checked-in default 4 remains known configuration risk, not a new key.
F60 PERF factor=1 makes glow surfaces approximately eye-sized and is more expensive than factor=4; preserve R51 pacing via measured HMD comparison.
CP60 fresh=60 carry=0 total=60.
F61 CROSS R30CaptureSkyGlowSceneBeforeHud requires world stereo, duplicated draw, no right failure/incomplete frame, BackBuffer and RightEyeSurface before copying either eye.
F62 ADV capture epoch suppresses repeated same-Present capture, falsifying unconditional multiple StretchRect copies per frame in this helper.
F63 PATH capture performs one StretchRect per eye into reduced texture and marks epoch only if both succeed.
F64 REG pre-HUD capture ordering is essential to prevent HUD contamination of glow; exact visual result remains NEED_HMD_TEST.
F65 PERF successful capture contributes two eye copies per eligible frame; at SkyGlowFactor=1 these are full-eye StretchRect costs and should be included in pacing counters.
F66 CROSS R30DrawSkyGlowPass explicitly changes RT, depth, viewport, shaders, texture, sampler and blend/color-write state, making state restoration around caller a critical boundary.
F67 ADV draw pass disables Z/stencil/alpha-test and optionally additive blends; failure after partial state mutation cannot be treated as harmless without caller restoration.
F68 PATH DrawPrimitiveUP triangle strip uses four XYZRHW vertices and detaches texture0 after draw, but does not locally restore every mutated state.
F69 REG RT/state restoration must therefore be verified at R30ApplyStereoSkyGlow caller/state-block boundary before any C change; no standalone defect promoted from helper alone.
F70 PERF each glow pass is immediate DrawPrimitiveUP plus shader sampling; aggregate pass count, copies and state changes need HMD frame-time evidence.
CP70 fresh=70 carry=0 total=70.
F71 CROSS Bright shader uses luminance threshold plus alpha-derived worldMask; alpha is explicitly not eye identity and cannot replace semantic HUD classification.
F72 ADV opaque world pixels retain minimum worldMask 0.15, so the mask suppresses rather than guarantees removal of HUD contamination; pre-HUD capture remains primary correctness boundary.
F73 PATH blur shader uses five taps and composite is additive RGB-only, giving a bounded per-pixel cost model for factor comparisons.
F74 REG SkyGlow visual intensity/flare-like contamination cannot be declared correct statically; preserve factor=1 and request Quest3/VDXR comparison.
F75 PERF B29 conclusion: no new static P0/P1 rendering defect promoted from buffer-shadow/XYZRHW/sky-glow review; primary actionable handoff remains existing R51 HUD/rank semantic restoration plus SkyGlow config policy, all under protected frame-stability baseline.

## Identity-validated carry-forward units
C76 IDENTITY integration HEAD re-read exactly `539fecf218d39cea103da6ccc23f59eb7498185f`.
C77 IDENTITY integration commit message remains regression baseline qualification/open HUD defects; runtime tree identity unchanged this invocation.
C78 IDENTITY review-b prior HEAD re-read `dad2b6e4c96deda12cdb2ceb496d7ba03768b403` (B28).
C79 IDENTITY protected USER_RUNTIME baseline re-read exactly `17ad376bfdf7939f0851c0c629e4fa094a84f28a`.
C80 IDENTITY baseline package SHA256 remains `bf9f5b7c04520f1bf018764f54c1a915e019f4a21b18bf5fab061ce29790fbe7`.
CP80 fresh=75 carry=5 total=80.
C81 REGISTRY baseline status remains PARTIAL_ACCEPTED_WITH_OPEN_VISUAL_DEFECTS.
C82 REGISTRY VR-STARTUP-WHITE-001 remains USER_RUNTIME_VERIFIED protected startup invariant.
C83 REGISTRY protected world invariant remains correct road/background/vehicle 3D stereo.
C84 REGISTRY protected performance invariant remains improved frame stability.
C85 REGISTRY protected ownership invariant remains no broad world-to-HUD/finite-plane ownership.
C86 REGISTRY VR-R51-WHITE-HUD-DIPLOPIA-001 remains open.
C87 REGISTRY VR-R51-VEHICLE-RANK-ANCHOR-001 remains open.
C88 REGISTRY BUILD_VERIFIED still cannot advance USER_RUNTIME baseline.
C89 REGISTRY failed runtime test still preserves last known good baseline.
C90 REGISTRY runtime-visible DONE still requires matching USER_RUNTIME_VERIFIED evidence.
CP90 fresh=75 carry=15 total=90.
C91 PROTOCOL B remains review-only and persists only review evidence/checkpoints.
C92 PROTOCOL C owns isolated candidate source modification/build trigger.
C93 PROTOCOL D owns independent exact-SHA review/integration/package.
C94 PROTOCOL candidate runtime-risk changes require BASELINE_DELTA.
C95 PROTOCOL D must compare baseline->prospective and integration->prospective trees.
C96 PROTOCOL SkyGlowFactor=1 remains preserved correctness contract.
C97 PROTOCOL TargetRefreshRateHz=0/equivalent dynamic XR cadence remains preserved contract.
C98 PROTOCOL hard-coded 120Hz assumption remains prohibited.
C99 PROTOCOL monitor resolution as eye resolution remains prohibited.
C100 PROTOCOL frozen package immutability remains required during user testing.
CP100 fresh=75 carry=25 total=100.
C101 R30 buffer shadow maximum tracked buffer size remains 16MiB.
C102 R30 vertex shadow registry remains keyed by IDirect3DVertexBuffer9 pointer.
C103 R30 index shadow registry remains keyed by IDirect3DIndexBuffer9 pointer.
C104 R30 shadow registry has a global mutex.
C105 R30 each shadow entry has its own mutex.
C106 R30 DISCARD write clears validity.
C107 R30 READONLY lock does not become authoritative write data.
C108 R30 shadow miss fails open rather than GPU Locking in draw.
C109 R30 scratch storage remains thread_local.
C110 R30 scratch lease retains reentrancy busy guard.
CP110 fresh=75 carry=35 total=110.
C111 R30PrepareXyzrhwState requires stable stereo base.
C112 R30PrepareXyzrhwState requires fixed-function/no vertex shader identity.
C113 R30PrepareXyzrhwState requires XYZRHW FVF.
C114 R30PrepareXyzrhwState requires valid viewport/depth span.
C115 R30PrepareXyzrhwState requires current latched stereo frame.
C116 R30 pose-sequence mismatch rejects conversion.
C117 projected-depth signature requires coherent Z/RHW relation.
C118 projected-depth signature sampling remains capped at 512 vertices.
C119 depth-enabled draw without RHW evidence is not promoted to world effect.
C120 tracked active depth/stencil requires right-eye synchronization before world conversion.
CP120 fresh=75 carry=45 total=120.
C121 SkyGlow resources remain per-eye reduced/temp texture pairs.
C122 SkyGlow resource cache remains keyed by eye/glow dimensions and factor.
C123 SkyGlow factor remains clamped internally to 1..16.
C124 SkyGlow reduced size floor remains 160x120.
C125 sky capture still requires completed stereo/world eligibility.
C126 sky capture still copies both BackBuffer and RightEyeSurface.
C127 sky capture epoch still prevents repeat capture in same Present epoch.
C128 glow draw still uses DrawPrimitiveUP triangle strip.
C129 glow pass still mutates render/depth/viewport/shader/sampler/blend state.
C130 texture0 is detached after each glow draw helper call.
CP130 fresh=75 carry=55 total=130.
C131 Bright shader threshold remains peak>0.58 scaled by 2.75.
C132 Bright shader alpha worldMask floor remains 0.15.
C133 Blur shader remains five texture taps.
C134 Composite shader remains additive RGB with zero alpha output.
C135 checked-in SkyGlowFactor remains a policy-sensitive surface and must resolve to 1 for CORRECTNESS.
C136 runtime SkyGlow correctness remains NEED_HMD_TEST.
C137 runtime smoke/skid placement remains NEED_HMD_TEST.
C138 runtime XYZRHW HUD/world classification remains NEED_HMD_TEST where user-visible.
C139 runtime copy/wait/frame pacing remains governed by protected R51 stability evidence.
C140 no static evidence in B29 supersedes USER_RUNTIME_VERIFIED baseline.
CP140 fresh=75 carry=65 total=140.
C141 HISTORY Issue #13 remains append-only runtime regression ledger.
C142 HISTORY recurrent symptom must reuse stable regression key.
C143 DEDUP no new white-HUD key created; R51 key reused.
C144 DEDUP no new rank-anchor key created; R51 key reused.
C145 DEDUP no new SkyGlow key created from known config-policy mismatch.
C146 HANDOFF READY_FOR_C remains exact HUD producer SCREEN_HUD ownership plus exact rank WORLD_BILLBOARD ownership only.
C147 HANDOFF C must preserve R51 world stereo and frame stability and avoid blanket queue->HUD.
C148 HANDOFF C must preserve SkyGlowFactor=1 and dynamic refresh.
C149 HANDOFF runtime visual/performance claims remain NEED_HMD_TEST until matching Quest3/VDXR evidence.
C150 VERIFY physical evidence row 150; fresh=75 carry=75 total=150; production/candidate source writes=0.
CP150 fresh=75 carry=75 total=150 COMPLETE.

Diversity: cross-subsystem=15+, adversarial/falsification=15+ explicit ADV plus additional contrary checks; distinct functions/code paths=20+ (`R30BeginObservedLock`, `R30FinishObservedLock`, `R30MergeValidRange`, `R30RangeValid`, `R30CopyVertexShadow`, `R30CopyIndexShadow`, `R30EnsureIndexShadow`, `R30VertexBufferUnlockDest`, `R30VertexBufferReleaseDest`, `R30ScratchLease`, `R30PrepareXyzrhwState`, `R30XyzrhwHasProjectedDepthSignature`, `R30ConfigureXyzrhwWorldEffect`, matrix/full-world reprojection path, `R30EnsureSkyGlowResources`, `R30CaptureSkyGlowSceneBeforeHud`, `R30DrawSkyGlowPass`, Bright/Blur/Composite shader paths and state/copy boundaries). Fresh units are outside the most recently reviewed R51 semantic delta except baseline/regression joins.

C nextAction: from exact integration `539fecf`, preserve the R51 USER_RUNTIME baseline while implementing only evidence-backed exact white-HUD SCREEN_HUD and rank-marker WORLD_BILLBOARD ownership; keep buffer-shadow/XYZRHW conservative gates intact unless a deterministic failing verifier proves otherwise; ensure CORRECTNESS resolves `SkyGlowFactor=1`, preserve dynamic refresh, record dual BASELINE_DELTA, and keep user-visible visual/pacing outcomes NEED_HMD_TEST.