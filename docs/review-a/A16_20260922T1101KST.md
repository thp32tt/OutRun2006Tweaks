# Role-A A16 — 2026-09-22 11:01 KST

## Identity / regression gate
- integration: `d961afe38d6417961d65d7e23d3a65f33bcec39c`
- candidate: `b8bd82c3bdb5dfadb8e528752a9dba2a742cad51`
- exact base: `d961afe38d6417961d65d7e23d3a65f33bcec39c`
- candidate relation: ahead 2 / behind 0; merge-base == exact base
- changed production path: `src/hooks_graphics.cpp` (+4/-1)
- verifier: `tools/verify_vr_nearplane_active_gate.py` (+20)
- matched regression case: `VR-STARTUP-WHITE-001` (riskPath `src/hooks_graphics.cpp`)
- regression classification: EVIDENCE_AUGMENT; no recurrence symptom observed; case remains BUILD_VERIFIED_NEED_HMD_TEST.
- stored root cause consulted: CreateDevice-thread premature stereo resource initialization; this candidate does not modify CreateDevice/resource initialization/present-deferred-init ownership.

## Exact-SHA post-review verdict
**PASS_DOMAIN_REVIEW_NEED_BUILD_HMD_GATE** for A architecture/state/lifetime lens.

Bounded hypothesis implemented: VRNearPlane must only replace the normal Z precision near plane when positional tracking is enabled AND stereo is configured AND the shared runtime authority says stereo injection is currently eligible. Base used `VREnabled && VRPositionalTracking`; candidate uses `VRPositionalTracking && VRStereo && RuntimeEligibility::MayInjectStereo()` and imports the shared authority header.

Architecture evidence:
- `MayInjectStereo()` requires SafetyOverlayReady, HostFresh, HostRenderable, !ExternalSafetyBlock, StereoAllowed, and !RecoveryPending using acquire loads.
- Therefore startup/recovery/stale-host/reset-safety states fail closed to the existing non-VR near-plane branches rather than applying VRNearPlane.
- Scene-effect ownership is preserved: `allow_znear_override=false` bypasses both VR and normal override while `Clr_SceneEffect_dest` temporarily applies its 0.05 plane.
- Candidate does not mutate runtime eligibility state, host IPC, generation/ACK, reset/session lifetime, or deferred stereo-resource initialization.
- No new architecture regression finding promoted.

Validation gap: candidate has no commit status checks/workflow runs yet. Static exact-diff/domain PASS is not BUILD VERIFIED. Because `hooks_graphics.cpp` triggers `VR-STARTUP-WHITE-001`, D must carry its stored static/build recipe and Quest3/VDXR logo->menu/game->gameplay-stereo runtime gate; final regression DONE remains forbidden without USER RUNTIME VERIFIED evidence.

## 150-unit ledger
Rows A16-001..A16-120 are carry-forward from A15 only after validating that the integration runtime-source dependency identity remains unchanged at `d961afe...`; their evidence identities are unchanged. Rows A16-121..A16-150 are new/independent exact-candidate architecture traces/falsifications below. This produces exactly 150 evidence-bearing rows; existing-finding revalidation remains <=30.

| unit | kind | path/function | evidence / falsification objective | result | regression |
|---|---|---|---|---|---|
