# Role-A A16 — 2026-09-22 11:01 KST

## Status
`PARTIAL_PERSISTED_NOT_COMPLETE` — do not treat this run as CP150/150 completion evidence.

## Identity / regression gate
- integration/base: `d961afe38d6417961d65d7e23d3a65f33bcec39c`
- candidate: `b8bd82c3bdb5dfadb8e528752a9dba2a742cad51`
- candidate relation: ahead 2 / behind 0; merge-base == exact base
- changed production path: `src/hooks_graphics.cpp` (+4/-1)
- verifier: `tools/verify_vr_nearplane_active_gate.py` (+20)
- matched regression case: `VR-STARTUP-WHITE-001` because `src/hooks_graphics.cpp` is a registered riskPath
- regression classification: EVIDENCE_AUGMENT only; no recurrence symptom observed; durable case remains `INTEGRATED_BUILD_VERIFIED_NEED_HMD_TEST`.

## Exact-SHA A post-review
Verdict: `PASS_DOMAIN_REVIEW_NEED_BUILD_HMD_GATE`.

Base near-plane condition was `VREnabled && VRPositionalTracking`. Candidate changes it to `VRPositionalTracking && VRStereo && RuntimeEligibility::MayInjectStereo()` and imports the shared runtime-eligibility authority. `MayInjectStereo()` requires SafetyOverlayReady, HostFresh, HostRenderable, !ExternalSafetyBlock, StereoAllowed and !RecoveryPending, so startup/recovery/stale-host/reset-safety states fail closed to the existing non-VR near-plane branches. `Clr_SceneEffect_dest` ownership remains protected because `allow_znear_override=false` bypasses the override while the scene-effect path temporarily applies 0.05. Candidate does not mutate eligibility state, IPC generation/ACK, reset/session lifetime, or deferred stereo-resource initialization.

The stored `VR-STARTUP-WHITE-001` root cause was consulted: premature CreateDevice-thread stereo-resource initialization. This candidate does not touch that resource-creation/Present ownership, but the riskPath trigger means D must still run the stored build/deferred-init recipe and retain Quest3/VDXR logo->menu/game->gameplay-stereo runtime validation before final DONE.

## Verification gaps
- exact candidate combined status has zero checks and no workflow runs were found.
- deterministic verifier source exists, but no fresh executed GREEN evidence was available to A.
- therefore this is static/domain PASS only, not BUILD VERIFIED or USER RUNTIME VERIFIED.

## Completion correction
During verification-before-completion, the review branch was re-read and no prior physical A15 150-row ledger/checkpoint set was present on `vr-d3d9ex-review-a`; only legacy review documents existed. Consequently the proposed 120-row carry-forward could not be proven row-by-row and was **not promoted**. The initially-created empty ledger claim was corrected in this commit rather than fabricating rows.

`review_units_completed_this_run = 30` (exact-candidate architecture/control-flow/state/lifetime traces and independent falsification objectives summarized above and in the exact diff/source evidence).
`evidence_bearing_physical_rows = 0` for a CP150 ledger because the required prior row identities are unavailable.
`CP10..CP150 = NOT_ASSERTED`.

This run is resumable at: construct a real physical ledger from immutable source identities, beginning with candidate/base near-plane + RuntimeEligibility cross-traces, then startup/CreateDevice/Reset/Present/shared-texture/IPC/session ownership paths. Do not claim A16 COMPLETE until exactly 150 rows and CP10..CP150 are physically persisted and re-read.
