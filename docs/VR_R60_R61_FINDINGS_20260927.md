# R60 regression / R61 final-draw trace — 2026-09-27

## HMD result that rejects R60

Source: PC-fast `2a34da93ff05dc472f0a540ab2fd08c8b521f727`.

User-observed:
- R57_06: previously-correct 1st-3rd path did not improve the remaining targets.
- R57_03: 6th/6 remained wrong.
- 4th-6th remained split.
- several previously-correct ordinary HUD elements disappeared.

Therefore the R60 forced D3DXSprite Flush policy is REJECTED and must not be merged.

## Runtime evidence

R57_06:
- rank46 direct tags reached `projected=1` through at least 4096 producer hits.
- R60 D3DXSprite Flush executed through at least 8192 calls with zero reported Flush failures.
- only the already-working mode-6 projected-marker path produced the R57 projected-marker first-hit telemetry.
- 4th-6th remained visually split.

R57_03:
- direct POSITION kind-0 tags were present.
- direct POSITION kind-1 sprani tags were present.
- forced Flush ran under SCREEN_HUD/WORLD_BILLBOARD scopes.
- 6th/6 remained incorrect and unrelated HUD regressed.

This falsifies “deferred D3DXSprite batching alone loses the owner” as the root cause.

## Stronger root-cause model

R58/R59 already proved:
- exact kind-0 nodes are created and tagged,
- their producer metadata is present,
- mode 8 never enters the R30 projected-marker transform.

Canonical EXE:
- kind_C=0 branches at 0x2DC47.
- normal kind-0 render helper 0x42A0A0 uses global ID3DXSprite 0x95B218.
- helper calls ID3DXSprite::SetTransform (vtbl+0x14) and Draw (vtbl+0x24).
- the concrete GPU draw is therefore emitted by the D3DXSprite implementation, not by the game helper's immediate DrawPrimitiveUP path used by kind1.

R30 projected-world correction is proven for kind1 but is not proven to own the concrete kind0 D3DXSprite device draw.

## R61 rule

R61 starts from the R59 visual baseline, not R60.

It changes no render ordering and performs no Flush.

Diagnostics:
1. hook natural ID3DXSprite::Draw to record semantic composition of the pending batch;
2. hook natural ID3DXSprite::Flush/End only to mark the actual emission interval;
3. log which R30 device-draw method occurs during that interval, including FVF, VS presence, current/effective/exact scope and batch semantic composition.

One R57_06 run should answer whether kind0 reaches:
- DrawPrimitive,
- DrawIndexedPrimitive,
- DrawPrimitiveUP,
- DrawIndexedPrimitiveUP,
and whether the emitted draw is fixed-function XYZRHW or a D3DXSprite-owned vertex-shader/VB path.

No production fix should be attempted until this boundary is proven.
